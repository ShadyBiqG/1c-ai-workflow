# Довести 1C AI Workflow до строгого production-ready процесса

Исправить недостатки, выявленные при аудите задач `TASK-20260916-042033-c94c` и `TASK-20260916-012049-7de5` из проекта `F:\erGitBase\114`.

Обязательные цели:

- ввести детерминированный и проверяемый расчёт сложности с evidence, floor-триггерами и реклассификацией;
- хранить исходный пользовательский запрос неизменным отдельно от структурированной интерпретации требований;
- обеспечить трассировку request -> architecture -> plan -> work items -> tests -> verify;
- запрещать Architect и Manager вводить новое бизнес-поведение без подтверждённого требования или явного решения пользователя;
- добавить scope-change gate для материальных неоднозначностей и расширения области;
- расширить независимое архитектурное ревью обязательной проверкой соответствия запросу;
- разделить baseline грязного рабочего дерева и изменения конкретной задачи, включая изменения внутри уже грязных файлов;
- проверять task delta, а не весь worktree, и обнаруживать восстановление ранее удалённых строк, объектов и legacy-фрагментов;
- поддержать отрицательные инварианты `forbidden_paths`, `forbidden_symbols`, `must_remain_deleted`;
- сузить ownership work items до точных путей и операций, валидировать пересечения, количество items и безопасную параллельность;
- исправить семантику human/agent/implicit approval и запретить агенту подделывать человеческое подтверждение;
- сохранять все итерации архитектуры и ревью без перезаписи evidence;
- устранить дефекты VERIFY: PowerShell compatibility, однозначный JSON/YAML contract, корректный scope тестов, structured review completion, baseline-aware XML и импорт внешнего evidence;
- добавить состояния/гейты для manual UI, infrastructure failure и контролируемой финализации без подмены FAIL на PASS;
- запрещать summary/final delivery до допустимого терминального состояния;
- добавить регрессионные Pester-тесты на реальные инциденты, включая грязный worktree, same-file legacy restoration, scope drift, неверную сложность и ложное approval;
- обновить документацию, схемы, установщик и устанавливаемые копии согласованно.

Сохранить все существующие пользовательские изменения. Не выполнять commit, deploy или публикацию.
