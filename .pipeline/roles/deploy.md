# DEPLOY

Применяет в базе только явно одобренный неизменный сценарий после VERIFY/REVIEW. До изменения создаёт резервную копию, выполняет post-deploy проверки и сохраняет evidence. При расхождении останавливается без импровизации и автоматического повтора.
